<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Java | Tomasz Dziurko]]></title>
  <link href="http://tomaszdziurko.pl/blog/categories/java/atom.xml" rel="self"/>
  <link href="http://tomaszdziurko.pl/"/>
  <updated>2015-11-23T08:12:02+01:00</updated>
  <id>http://tomaszdziurko.pl/</id>
  <author>
    <name><![CDATA[Tomasz Dziurko]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[One and the only one reason to customize IntelliJ IDEA memory settings]]></title>
    <link href="http://tomaszdziurko.pl/2015/11/1-and-the-only-one-to-customize-intellij-idea-memory-settings/"/>
    <updated>2015-11-23T08:00:10+01:00</updated>
    <id>http://tomaszdziurko.pl/2015/11/1-and-the-only-one-to-customize-intellij-idea-memory-settings</id>
    <content type="html"><![CDATA[<p><em>Don&rsquo;t be a Scrooge and give your IDE some more memory</em></p>

<!-- more -->


<p>Yesterday we had a discussion about customizing memory settings in IntelliJ IDEA and it appeared that some people do not do that, some people (like me) use some simple changeset
and some developers craft a fancy, sophisticated setup that satisfy their needs. While working for a current project I had to deal with several small microservices projects and
one older project, a core of the client&rsquo;s business which is quite big. And after applying some changes I have noticed really significant improvement when it comes to IDE speed and responsiveness.
But at that time I didn&rsquo;t measure what exactly changed, it was only a subjective observation that <em>&ldquo;IDEA is now faster&rdquo;</em>.</p>

<p>But during that discussion one of the developers working for the same client (thank you Yuri) sent me his settings and I was really overwhelmed by their level of complexity.
I was happy with my own setup, but I was also curious how those completely different settings compare to each other and also how they compare to defaults provided by JetBrains. So after
discussion with Yuri I knew that this is a good material for a blog post.</p>

<h1>Goal</h1>

<p>My plan is to compare how different IDEA memory settings perform in a scenario close to my daily usage (load big project, load two or three microservices, refresh big project after hypothetical <em>git pull</em>)
and then choose the most optimal settings when it comes to memory consumption and speed improvement.</p>

<h1>Test machine and projects</h1>

<p><strong>Laptop</strong>: MacBook Pro Retina, 2,3GHz Intel Core i7, 16GB 1600Mhz DDR3, SSD Disc, OS X Yosemite</p>

<p><strong>Projects</strong></p>

<ul>
<li>Big Project  &ndash; monolith, 700 000 lines of code (Java 8 and Groovy), 303 Gradle modules</li>
<li>Two microservices &ndash; small projects with 10 000 &ndash; 20 000 lines of code (Java 8 and Groovy), each with one Gradle module</li>
</ul>


<h1>Test scenario</h1>

<ol>
<li>Close all projects in Idea</li>
<li>Put settings under test to idea.vmoptions file</li>
<li>Reset my laptop</li>
<li>After a start close all unrelated programs (communicators, etc.)</li>
<li>Open Idea (measure time)</li>
<li>Open Big Project (measure time)</li>
<li>Check jstat -gcutil</li>
<li>Open two more projects: Microservice One and Microservice Two (measure times)</li>
<li>Check jstat -gcutil</li>
<li>Go back to Big Project and click &ldquo;Refresh Gradle project&rdquo; button (measure time)</li>
<li>Check jstat -gcutil</li>
</ol>


<h2>What is jstat -gcutil</h2>

<p>jstat is a tool available in your JDK to monitor JVM and Garbage Collector statistics. It has many different options to collect various data
(<a href="https://docs.oracle.com/javase/8/docs/technotes/tools/unix/jstat.html">full documentation is here</a>) but we are interested in only one: <strong>-gcutil</strong>:</p>

<p>```</p>

<pre><code>-gcutil - Summary of garbage collection statistics.

S0: Survivor space 0 utilization as a percentage of the space's current capacity.
S1: Survivor space 1 utilization as a percentage of the space's current capacity.
E: Eden space utilization as a percentage of the space's current capacity.
O: Old space utilization as a percentage of the space's current capacity.
M: Metaspace utilization as a percentage of the space's current capacity.
CCS: Compressed class space utilization as a percentage.
YGC: Number of young generation GC events.
YGCT: Young generation garbage collection time.
FGC: Number of full GC events.
FGCT: Full garbage collection time.
GCT: Total garbage collection time.
</code></pre>

<p>```</p>

<p>Example output from this command looks like this:
```</p>

<pre><code>S0     S1    E     O     M    CCS  YGC YGCT FGC  FGCT   GCT
89.70 0.00 81.26 74.27 95.68 91.76 40 2.444 14  0.715  3.159
</code></pre>

<p>```</p>

<p>In this post the most important parameters are number of GC events (<strong>YGC</strong> and <strong>FGC</strong>) and collection times (<strong>YGCT</strong> and <strong>FGCT</strong>).</p>

<h1>Tested settings</h1>

<p>I have tested four different settings, to make reading easier each of them was given a name.</p>

<h2>Default (grey)</h2>

<p>These are built-in settings provided by JetBrains, clean IDEA 15 is using them:</p>

<p>```</p>

<pre><code>-Xms128m
-Xmx750m
-XX:MaxPermSize=350m
-XX:ReservedCodeCacheSize=240m
-XX:+UseCompressedOops
</code></pre>

<p>```</p>

<h2>Big (red)</h2>

<p>4096MB for Xmx and 1024MB for <em>ReservedCodeCacheSize</em>, that&rsquo;s quite a lot of memory.</p>

<p>```</p>

<pre><code>-Xms1024m
-Xmx4096m
-XX:ReservedCodeCacheSize=1024m
-XX:+:UseCompressedOops
</code></pre>

<p>```</p>

<h2>Balanced (blue)</h2>

<p>2GB for Xmx and 2GB for Xms, more balanced approach to the memory consumption</p>

<p>```</p>

<pre><code>-Xms2g
-Xmx2g
-XX:ReservedCodeCacheSize=1024m
-XX:+:UseCompressedOops
</code></pre>

<p>```</p>

<h2>Sophisticated (orange)</h2>

<p>2GB for Xmx and 2GB for Xms as above, but different Garbage Collector is specified and many different flags for GC and memory management. I have received these settings from Yuri.</p>

<p>```</p>

<pre><code>-server
-Xms2g
-Xmx2g
-XX:NewRatio=3
-Xss16m
-XX:+UseConcMarkSweepGC
-XX:+CMSParallelRemarkEnabled
-XX:ConcGCThreads=4
-XX:ReservedCodeCacheSize=240m
-XX:+AlwaysPreTouch
-XX:+TieredCompilation
-XX:+UseCompressedOops
-XX:SoftRefLRUPolicyMSPerMB=50
-Dsun.io.useCanonCaches=false
-Djava.net.preferIPv4Stack=true
-Djsse.enableSNIExtension=false
-ea
</code></pre>

<p>```</p>

<p>So these are our test setups. To perform our test scenarios we have to create a file <em>idea.vmoptions</em> under <em>~/Library/Preferences/IntelliJIdea15/</em> (it is Mac OS specific,
to see how change these settings for your OS, check <a href="https://www.jetbrains.com/idea/help/increasing-memory-heap.html">this article</a>).</p>

<p>Now it is time to perform our test scenarios and compare the results.</p>

<h1>Results</h1>

<h2>Idea startup time</h2>

<p><img class="center <a" src="href="https://docs.google.com/spreadsheets/d/1iN6_z2HfJPsGPKlNzwgnUxps4MT5BxhUbOuhwXdnIgI/pubchart?oid=1557289692&amp;format=image">https://docs.google.com/spreadsheets/d/1iN6_z2HfJPsGPKlNzwgnUxps4MT5BxhUbOuhwXdnIgI/pubchart?oid=1557289692&amp;format=image</a>" width="800" height="500"></p>

<p>As you can see startup time does not depend on memory settings. Idea startup time is about 10 seconds for all test scenarios no matter how many memory we allocate.
And this is not a surprise as those settings should not affect application behaviour in such early stage.</p>

<h2>Time to load The Big Project</h2>

<p>Ok, so now it is time to load our Monolith and its 700k lines of code.</p>

<p><img class="center <a" src="href="https://docs.google.com/spreadsheets/d/1iN6_z2HfJPsGPKlNzwgnUxps4MT5BxhUbOuhwXdnIgI/pubchart?oid=1400772564&amp;format=image">https://docs.google.com/spreadsheets/d/1iN6_z2HfJPsGPKlNzwgnUxps4MT5BxhUbOuhwXdnIgI/pubchart?oid=1400772564&amp;format=image</a>" width="800" height="500"></p>

<p>Finally there are some differences. Default settings performs almost three times worse than the rest. Apparently so large codebase needs more memory :) And if we execute</p>

<p>```</p>

<pre><code>jstat -gcutil &lt;IDEA_PID&gt;
</code></pre>

<p>```</p>

<p>we will notice that Garbage Collector was really, really busy with default settings when we compare it other setups.</p>

<p><img class="center <a" src="href="https://docs.google.com/spreadsheets/d/1iN6_z2HfJPsGPKlNzwgnUxps4MT5BxhUbOuhwXdnIgI/pubchart?oid=93956661&amp;format=image">https://docs.google.com/spreadsheets/d/1iN6_z2HfJPsGPKlNzwgnUxps4MT5BxhUbOuhwXdnIgI/pubchart?oid=93956661&amp;format=image</a>" width="800" height="500"></p>

<p><img class="center <a" src="href="https://docs.google.com/spreadsheets/d/1iN6_z2HfJPsGPKlNzwgnUxps4MT5BxhUbOuhwXdnIgI/pubchart?oid=1213605225&amp;format=image">https://docs.google.com/spreadsheets/d/1iN6_z2HfJPsGPKlNzwgnUxps4MT5BxhUbOuhwXdnIgI/pubchart?oid=1213605225&amp;format=image</a>" width="800" height="500"></p>

<p>Not only total time spent by GC on freeing memory is dramatically higher (about 50 times higher) but also average execution time of Full GC is much, much longer. Long periods spent on Full GCs
are the main cause of low responsiveness of our IDE.</p>

<h2>Opening two microservices in IDEA</h2>

<p>Ok, so we have our Monolith loaded but we need to add some code to two smaller microservices. So let&rsquo;s open them in IDEA and compare total times.</p>

<p><img class="center <a" src="href="https://docs.google.com/spreadsheets/d/1iN6_z2HfJPsGPKlNzwgnUxps4MT5BxhUbOuhwXdnIgI/pubchart?oid=2053860050&amp;format=image">https://docs.google.com/spreadsheets/d/1iN6_z2HfJPsGPKlNzwgnUxps4MT5BxhUbOuhwXdnIgI/pubchart?oid=2053860050&amp;format=image</a>" width="800" height="500"></p>

<p>In this test scenario we see that differences are still visible and <strong>Sophisticated</strong> wins here, <strong>Default</strong> is far behind the rest.</p>

<h3>jstat -gcutil again</h3>

<p>After loading two microservices we can check how Garbage Collectors is performing with three opened projects. We can see that three custom settings
look almost the same and results of default settings are very, very poor.</p>

<p><img class="center <a" src="href="https://docs.google.com/spreadsheets/d/1iN6_z2HfJPsGPKlNzwgnUxps4MT5BxhUbOuhwXdnIgI/pubchart?oid=242159654&amp;format=image">https://docs.google.com/spreadsheets/d/1iN6_z2HfJPsGPKlNzwgnUxps4MT5BxhUbOuhwXdnIgI/pubchart?oid=242159654&amp;format=image</a>" width="800" height="500"></p>

<p><img class="center <a" src="href="https://docs.google.com/spreadsheets/d/1iN6_z2HfJPsGPKlNzwgnUxps4MT5BxhUbOuhwXdnIgI/pubchart?oid=1738350771&amp;format=image">https://docs.google.com/spreadsheets/d/1iN6_z2HfJPsGPKlNzwgnUxps4MT5BxhUbOuhwXdnIgI/pubchart?oid=1738350771&amp;format=image</a>" width="800" height="500"></p>

<h2>Final stage: reload the Monolith</h2>

<p>So we have coded for a while and now we need to fetch latest version of The Big Project from repository and then refresh Gradle Project so IDEA
could see all new classes, etc.</p>

<p><img class="center <a" src="href="https://docs.google.com/spreadsheets/d/1iN6_z2HfJPsGPKlNzwgnUxps4MT5BxhUbOuhwXdnIgI/pubchart?oid=625856708&amp;format=image">https://docs.google.com/spreadsheets/d/1iN6_z2HfJPsGPKlNzwgnUxps4MT5BxhUbOuhwXdnIgI/pubchart?oid=625856708&amp;format=image</a>" width="800" height="500"></p>

<p><strong>Important note</strong>: Bar for Default setup is so high because IDEA crashed during refresh and I could not measure the actual time. Simply assigned memory was
not sufficient to perform this operation.</p>

<p>But looking at the best three, we see that Big settings refreshed project in the shortest time so the largest assigned memory helps a bit here.</p>

<h3>jstat -gcutil for the last time</h3>

<p>Because IDEA was not able to refresh projet with <strong>Default</strong> settings, they are not included in this measurement.</p>

<p><img class="center <a" src="href="https://docs.google.com/spreadsheets/d/1iN6_z2HfJPsGPKlNzwgnUxps4MT5BxhUbOuhwXdnIgI/pubchart?oid=1467101172&amp;format=image">https://docs.google.com/spreadsheets/d/1iN6_z2HfJPsGPKlNzwgnUxps4MT5BxhUbOuhwXdnIgI/pubchart?oid=1467101172&amp;format=image</a>" width="800" height="500"></p>

<p><img class="center <a" src="href="https://docs.google.com/spreadsheets/d/1iN6_z2HfJPsGPKlNzwgnUxps4MT5BxhUbOuhwXdnIgI/pubchart?oid=712785115&amp;format=image">https://docs.google.com/spreadsheets/d/1iN6_z2HfJPsGPKlNzwgnUxps4MT5BxhUbOuhwXdnIgI/pubchart?oid=712785115&amp;format=image</a>" width="800" height="500"></p>

<p>In these last charts we see that differences between total times are quite small, but single Full GC is the fastest with <strong>Big</strong> settings applied. Again,
it looks like very large Xmx helps a bit more when it comes to responsiveness.</p>

<h1>Summary</h1>

<p>In this short experiment I tried to test how much we could gain by customising memory settings of IntelliJ IDEA and it looks like that even some simple
tweaks can drastically improve performance of our IDE and speed up our work. Of course the more memory you assign, the better results you will see, but
we are using IDE next to many different applications which also consume memory so our goal should be to find a balance between performance gain and memory consumption.
I think that in most cases setting <strong>Xmx</strong> to value between 2g and 3g is the best approach. If you have some more time you can play with jstat and
<a href="http://docs.oracle.com/javase/6/docs/technotes/tools/share/jvisualvm.html">jvisualm</a> to check how changing different VM flags affect performance
and memory footprint.</p>

<h3>And you?</h3>

<p>And what about you? What is your idea.vmoptions setup? Do you have any other ways to improve InteliJ IDEA performance?</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Speaker at Java Developers' Day 2015]]></title>
    <link href="http://tomaszdziurko.pl/2015/10/speaker-at-java-developers-day-2015/"/>
    <updated>2015-10-07T03:00:10+02:00</updated>
    <id>http://tomaszdziurko.pl/2015/10/speaker-at-java-developers-day-2015</id>
    <content type="html"><![CDATA[<p>Showing 1011 mistakes developers make during recruitment process</p>

<!-- more -->


<p><img class="center" src="/images/blog/2015/10/jdd_speaker.png" width="400" height="400"></p>

<p>Next week I will be speaking at <a href="http://15.jdd.org.pl">Java Developer&rsquo;s Day 2015</a> in Cracow. So if you are planning to change your job in the near future or just want to know what mistakes developers make during
recruitment process, fell invited to my talk (in Polish unfortunately) during <a href="http://15.jdd.org.pl/agenda/agenda/13/">2nd day of conference, at 9am</a>.</p>

<p>During my presentation I will tell you about the most frequent and the most ridiculous mistakes during recruitment process in <a href="http://softwaremill.com">SoftwareMill</a> where I am one of the people responsible for hiring talented developers.
Surprisingly these errors quite often could be mapped to mistakes that developers make at work: either when they code or during other activities during work hours. So expect rather light talk without hardcore coding, with plenty of jokes and
funny situations from real life of developer-recruiter.</p>

<p><img class="center" src="/images/blog/2015/10/jdd_talk_start.png" width="900" height="600"></p>

<h2>Plans for the future</h2>

<p>But if you have different plans or simply can&rsquo;t attend JDD, I will be presenting this talk at Poznan JUG in January 2016 and probably also in Łódź and Warsaw in next few months. So if you want to discuss or ask something, please catch me during these events.</p>

<p>See you at <a href="http://15.jdd.org.pl">JDD</a>!</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Transparently persist and retrieve encrypted data from database]]></title>
    <link href="http://tomaszdziurko.pl/2013/07/transparently-persist-retrieve-encrypted-data-database/"/>
    <updated>2013-07-31T08:45:07+02:00</updated>
    <id>http://tomaszdziurko.pl/2013/07/transparently-persist-retrieve-encrypted-data-database</id>
    <content type="html"><![CDATA[<p>Big brother is watching&hellip; always so why not to encrypt everything. And then, auto-decrypt and encrypt our data stored in Database? Here comes Jasypt.</p>

<!-- more -->


<p>It&rsquo;s over two months since my last post here but June and July were extremely busy and intensive months this year. First, organisation of <a href="http://confitura.pl">Confitura</a> (the biggest free conference for Java developers in Europe) was taking all of my free evenings and then, after quite nervous period in hospital, our 2nd son was born. But now, I will try to blog regularly again, so please stay tuned.</p>

<p>In this post I will shortly describe how we can store encrypted data in our database and retrieve it as already decrypted in a simple, transparent way using <a href="http://www.jasypt.org/">Jasypt</a> library. Our use case will be to store Twitter Api credentials so they are safe in our database but still easy to retrieve and use to post updates in our timeline.</p>

<p>So what we have is a simple entity representing our settings item:
``` java SettingsItem entity
@Entity
public class SettingsItem implements Serializable {</p>

<pre><code>@Id
@GeneratedValue(strategy = javax.persistence.GenerationType.AUTO)
private Integer id;

private String name;
private String encryptedValue;
</code></pre>

<p>}
```</p>

<p>In such table we will store values for Twitter Consumer Key, Twitter Access Token and so on.</p>

<p>What we would like to achieve is that when we create SettingItem object with value as plain text and then we persist it, encryption is automatically performed so in the DB we have encrypted String. And of course, when we retrieve data from DB, we want to see decrypted String without additional effort, just out of the box.</p>

<h3>Jasypt to the rescue</h3>

<p style="text-align:center;"><img src="/images/blog/2013/07/jasypt-small.png" alt="jasypt-small" /></p>

<p><a href="http://www.jasypt.org">Jasypt</a> is a simple encryption library written in Java. It frees developer from dealing with low level configuration details and makes whole encryption process easy and straightforward. And what is most interesting now, it also has nice integration with Hibernate allowing seamless encryption/decryption of data stored in database.</p>

<h4>Setup</h4>

<p>To use Jasypt and its Hibernate integration module we have to add two items to our pom:
``` xml</p>

<pre><code>&lt;dependency&gt;
    &lt;groupId&gt;org.jasypt&lt;/groupId&gt;
    &lt;artifactId&gt;jasypt&lt;/artifactId&gt;
    &lt;version&gt;1.9.0&lt;/version&gt;
&lt;/dependency&gt;
&lt;dependency&gt;
    &lt;groupId&gt;org.jasypt&lt;/groupId&gt;
    &lt;artifactId&gt;jasypt-hibernate4&lt;/artifactId&gt;
    &lt;version&gt;1.9.0&lt;/version&gt;
&lt;/dependency&gt;
</code></pre>

<p>```</p>

<h4>Custom type</h4>

<p>Then we have to declare custom Hibernate type (@TypeDef) in our entity:</p>

<p>``` java
@TypeDef(</p>

<pre><code>    name="encryptedString",
    typeClass=EncryptedStringType.class,
    parameters= {
            // value will be used later to register encryptor
            @Parameter(name="encryptorRegisteredName", value="STRING_ENCRYPTOR")
    }
</code></pre>

<p>)
@Entity
public class SettingsItem implements Serializable {
   // (&hellip;)
}
```</p>

<p>and after that in the same class we can mark our encryptedValue field to use this custom type:</p>

<p>``` java</p>

<pre><code>@Type(type="encryptedString")
private String encryptedValue;
</code></pre>

<p>```</p>

<h4>Registering encryptor</h4>

<p>We are almost done. Last thing we have to do is register encryptor in HibernatePBEEncryptorRegistry class. This can be done in  initialization class of our application, e.g. ServletContext or simply in class with main(String[] args) method:</p>

<p>``` java</p>

<pre><code>String password = System.getProperty("jasypt.password");

StandardPBEStringEncryptor strongEncryptor = new StandardPBEStringEncryptor();
strongEncryptor.setPassword(password);
HibernatePBEEncryptorRegistry registry =
        HibernatePBEEncryptorRegistry.getInstance();
registry.registerPBEStringEncryptor("STRING_ENCRYPTOR", strongEncryptor);
</code></pre>

<p>```</p>

<p>One important thing here is that by using System.getProperty() or System.getenv() we can safely configure our encryption mechanism, password is provided at runtime by setting proper value on server machine.</p>

<h4>Summary</h4>

<p>As as a summary, one short passing test showing that our solution works:</p>

<p>``` java
public class SettingsItemRepositoryShould extends IntegrationTest {</p>

<pre><code>@Autowired
private SettingsItemRepository repository;


@BeforeClass
public static void init() {
    StandardPBEStringEncryptor strongEncryptor = new StandardPBEStringEncryptor();
    strongEncryptor.setPassword("JohnDoe");

    HibernatePBEEncryptorRegistry registry =
            HibernatePBEEncryptorRegistry.getInstance();
    registry.registerPBEStringEncryptor("STRING_ENCRYPTOR", strongEncryptor);
}

@Test
public void shouldEncryptAndDecryptValue() {
    // Given
    String settingName = "test";
    String value = "EncryptMe";

    // When
    repository.save(new SettingsItem(settingName, value));

    // Then
    SettingsItem settingsItem = repository.findByName(settingName);
    assertThat(settingsItem.getEncryptedValue()).isEqualTo(value);
}
</code></pre>

<p>}
```</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[XStream - XStreamely easy way to work with XML data in Java]]></title>
    <link href="http://tomaszdziurko.pl/2013/04/xstream-xstreamely-easy-work-xml-data-java/"/>
    <updated>2013-04-22T10:55:55+02:00</updated>
    <id>http://tomaszdziurko.pl/2013/04/xstream-xstreamely-easy-work-xml-data-java</id>
    <content type="html"><![CDATA[<!-- more -->


<p>From time to time there is a moment when we have to deal with XML data. And most of the time it is not the happiest day in our life. There is even a term &ldquo;XML hell&rdquo; describing situation when programmer has to deal with many XML configuration files that are hard to comprehend. But, like it or not, sometimes we have no choice, mostly because specification from client says something like &ldquo;use configuration written in XML file&rdquo; or something similar. And in such cases, <a href="http://xstream.codehaus.org/">XStream</a> comes with its very cool features that make dealing with XML really less painful.</p>

<p style="text-align:center;"><a href="/images/blog/2013/04/xstream.gif"><img src="/images/blog/2013/04/xstream.gif" alt="xstream" /></a></p>

<h3>Overview</h3>

<p><a href="http://xstream.codehaus.org/">XStream</a> is a small library to serialize data between Java objects and XML. It&rsquo;s lightweight, small, has nice API and what is most important, it works with and without custom annotations that we might be not allowed to add when we are not the owner of Java classes.</p>

<h3>First example</h3>

<p>Suppose we have a requirement to load configuration from xml file:
``` xml
<config></p>

<pre><code>&lt;inputFile&gt;/Users/tomek/work/mystuff/input.csv&lt;/inputFile&gt;
&lt;truststoreFile&gt;/Users/tomek/work/mystuff/truststore.ts&lt;/truststoreFile&gt;
&lt;keystoreFile&gt;/Users/tomek/work/mystuff/CN-user.jks&lt;/keystoreFile&gt;

&lt;!-- ssl stores passwords--&gt;
&lt;truststorePassword&gt;password&lt;/truststorePassword&gt;
&lt;keystorePassword&gt;password&lt;/keystorePassword&gt;

&lt;!-- user credentials --&gt;
&lt;user&gt;user&lt;/user&gt;
&lt;password&gt;secret&lt;/password&gt;
</code></pre>

<p></config>
```</p>

<p>And we want to load it into Configuration object:
``` java
public class Configuration {</p>

<pre><code>private String inputFile;
private String user;
private String password;

private String truststoreFile;
private String keystoreFile;
private String keystorePassword;
private String truststorePassword;

// getters, setters, etc.
</code></pre>

<p>}
<code>
So basically what we have to do is:
</code> java</p>

<pre><code>FileReader fileReader = new FileReader("config.xml");  // load our xml file  
XStream xstream = new XStream();     // init XStream
// define root alias so XStream knows which element and which class are equivalent
xstream.alias("config", Configuration.class);    
Configuration loadedConfig = (Configuration) xstream.fromXML(fileReader);
</code></pre>

<p>```</p>

<p>And that&rsquo;s all, easy peasy :)</p>

<h3>Something more serious</h3>

<p>Ok, but previous example is very basic so now let&rsquo;s do something more complicated: real XML returned by real WebService.</p>

<p>``` xml
<DATA xmlns=""></p>

<pre><code>&lt;BAN&gt;
    &lt;UPDATED_AT&gt;2013-03-09&lt;/UPDATED_AT&gt;
    &lt;TROUBLEMAKER&gt;
        &lt;NAME1&gt;JOHN&lt;/NAME1&gt;
        &lt;NAME2&gt;EXAMPLE&lt;/NAME2&gt;
        &lt;AGE&gt;24&lt;/AGE&gt;
        &lt;NUMBER&gt;ASD123123&lt;/NUMBER&gt;
    &lt;/TROUBLEMAKER&gt;
&lt;/BAN&gt;
&lt;BAN&gt;
    &lt;UPDATED_AT&gt;2012-03-10&lt;/UPDATED_AT&gt;
    &lt;TROUBLEMAKER&gt;
        &lt;NAME1&gt;ANNA&lt;/NAME1&gt;
        &lt;NAME2&gt;BAKER&lt;/NAME2&gt;
        &lt;AGE&gt;26&lt;/AGE&gt;
        &lt;NUMBER&gt;AXN567890&lt;/NUMBER&gt;
    &lt;/TROUBLEMAKER&gt;
&lt;/BAN&gt;
&lt;BAN&gt;
    &lt;UPDATED_AT&gt;2010-12-05&lt;/UPDATED_AT&gt;
    &lt;TROUBLEMAKER&gt;
        &lt;NAME1&gt;TOM&lt;/NAME1&gt;
        &lt;NAME2&gt;MEADOW&lt;/NAME2&gt;
        &lt;NUMBER&gt;SGH08945&lt;/NUMBER&gt;
        &lt;AGE&gt;48&lt;/AGE&gt;
    &lt;/TROUBLEMAKER&gt;
&lt;/BAN&gt;
</code></pre>

<p></DATA>
```</p>

<p>What we have here is simple list of bans written in XML. We want to load it into collection of Ban objects. So let&rsquo;s prepare some classes (getters/setters/toString omitted):
``` java
public class Data {</p>

<pre><code>private List bans = new ArrayList(); 
</code></pre>

<p>}</p>

<p>public class Ban {</p>

<pre><code>private String dateOfUpdate;
private Person person;
</code></pre>

<p>}</p>

<p>public class Person {</p>

<pre><code>private String firstName;
private String lastName;
private int age;
private String documentNumber;
</code></pre>

<p>}
```</p>

<p>As you can see there is some naming and type mismatch between XML and Java classes (e.g. field name1->firstName, dateOfUpdate is String not a Date), but it&rsquo;s here for some example purposes.</p>

<p>So the goal here is to parse XML and get Data object with populated collection of Ban instances containing correct data. Let&rsquo;s see how it can be achieved.</p>

<h4>Parse with annotations</h4>

<p>First, easier way is to use annotations. And that&rsquo;s the suggested approach in situation when we can modify Java classes to which XML will be mapped.
So we have:
``` java
@XStreamAlias(&ldquo;DATA&rdquo;) // maps DATA element in XML to this class
public class Data {</p>

<pre><code>// Here is something more complicated. If we have list of elements that are 
// not wrapped in a element representing a list (like we have in our XML: 
// multiple &lt;ban&gt; elements not wrapped inside &lt;bans&gt; collection, 
// we have to declare that we want to treat these elements as an implicit list 
// so they can be converted to List of objects. 
@XStreamImplicit(itemFieldName = "ban") 
private List bans = new ArrayList();
</code></pre>

<p>}</p>

<p>@XStreamAlias(&ldquo;BAN&rdquo;) // another mapping
public class Ban {</p>

<pre><code>/*
 We want to have different field names in Java classes so
 we define what element should be mapped to each field
*/
@XStreamAlias("UPDATED_AT") // 
private String dateOfUpdate;

@XStreamAlias("TROUBLEMAKER")
private Person person;
</code></pre>

<p>}</p>

<p>@XStreamAlias(&ldquo;TROUBLEMAKER&rdquo;)
public class Person {</p>

<pre><code>@XStreamAlias("NAME1")
private String firstName;

@XStreamAlias("NAME2")
private String lastName;

@XStreamAlias("AGE") // String will be auto converted to int value
private int age;

@XStreamAlias("NUMBER")
private String documentNumber;
</code></pre>

<p>```</p>

<p>And actual parsing logic is very short:</p>

<p>``` java</p>

<pre><code>FileReader reader = new FileReader("file.xml");  // load file

XStream xstream = new XStream();
xstream.processAnnotations(Data.class);     // inform XStream to parse annotations in Data class
xstream.processAnnotations(Ban.class);      // and in two other classes... 
xstream.processAnnotations(Person.class);   // we use for mappings
Data data = (Data) xstream.fromXML(reader); // parse

// Print some data to console to see if results are correct
System.out.println("Number of bans = " + data.getBans().size());
Ban firstBan = data.getBans().get(0);
System.out.println("First ban = " + firstBan.toString()); 
</code></pre>

<p>```</p>

<p>As you can see annotations are very easy to use and as a result final code is very concise. But what to do in situation when we can&rsquo;t modify mapping classes? We can use different approach that doesn&rsquo;t require any modifications in Java classes representing XML data.</p>

<h4>Parse without annotations</h4>

<p>When we can&rsquo;t enrich our model classes with annotations, there is another solution. We can define all mapping details using methods from XStream object:</p>

<p>``` java</p>

<pre><code>FileReader reader = new FileReader("file.xml");  // three first lines are easy, 
XStream xstream = new XStream();                 // same initialisation as in the 
xstream.alias("DATA", Data.class);               // basic example above

xstream.alias("BAN", Ban.class);             // two more aliases to map... 
xstream.alias("TROUBLEMAKER", Person.class); // between node names and classes

// We want to have different field names in Java classes so
// we have to use aliasField(&lt;fieldInXml&gt;, &lt;mappedJavaClass&gt;, &lt;mappedFieldInJavaClass&gt;)
xstream.aliasField("UPDATED_AT", Ban.class, "dateOfUpdate"); 
xstream.aliasField("TROUBLEMAKER", Ban.class, "person");

xstream.aliasField("NAME1", Person.class, "firstName");
xstream.aliasField("NAME2", Person.class, "lastName");
xstream.aliasField("AGE", Person.class, "age");  // notice here that XML will be auto-converted to int "age"
xstream.aliasField("NUMBER", Person.class, "documentNumber");

/*
 Another way to define implicit collection
*/ 
xstream.addImplicitCollection(Bans.class, "bans");

Data data = (Data) xstream.fromXML(reader);  // do the actual parsing

// let's print results to check if data was parsed
System.out.println("Number of bans = " + data.getBans().size());
Ban firstBan = data.getBans().get(0);
System.out.println("First ban = " + firstBan.toString());
</code></pre>

<p>```</p>

<p>As you can see XStream allows to easily convert more complicated XML structures into Java objects, it also gives a possibility to tune results by using different names if this from XML doesn&rsquo;t suit our needs.
But there is one thing should catch your attention: we are converting XML representing a Date into raw String which isn&rsquo;t quite what we would like to get as a result. That&rsquo;s why we will add converter to do some job for us.</p>

<h3>Using existing custom type converter</h3>

<p>XStream library comes with set of built converters for most common use cases. We will use DateConverter. So now our class for Ban looks like that:
``` java
public class Ban {</p>

<pre><code>private Date dateOfUpdate;
private Person person;
</code></pre>

<p>}
<code>
And to use DateConverter we simply have to register it with date format that we expect to appear in XML data:
</code> java
   xstream.registerConverter(new DateConverter(&ldquo;yyyy-MM-dd&rdquo;, new String[] {}));
```</p>

<p>and that&rsquo;s it. Now instead of String our object is populated with Date instance. Cool and easy! But what about classes and situations that aren&rsquo;t covered by existing converters? We could write our own.</p>

<h3>Writing custom converter from scratch</h3>

<p>Assume that instead of dateOfUpdate we want to know how many days ago update was done:
``` java
public class Ban {</p>

<pre><code>private int daysAgo;
private Person person;
</code></pre>

<p>}
<code>
Of course we could calculate it manually for each Ban object but using converter that will do this job for us looks more interesting. Our DaysAgoConverter must implement [Converter](http://xstream.codehaus.org/javadoc/com/thoughtworks/xstream/converters/Converter.html) interface so we have to implement three methods with signatures looking a little bit scary:
</code> java
public class DaysAgoConverter implements Converter {</p>

<pre><code>@Override
public void marshal(Object source, HierarchicalStreamWriter writer, MarshallingContext context) {
}

@Override
public Object unmarshal(HierarchicalStreamReader reader, UnmarshallingContext context) {
}

@Override
public boolean canConvert(Class type) {
    return false;
}
</code></pre>

<p>}
<code>
Last one is easy as we will convert only Integer class. But there are still two methods left with these HierarchicalStreamWriter, MarshallingContext, HierarchicalStreamReader and UnmarshallingContext parameters. Luckily, we could avoid dealing with them by using [AbstractSingleValueConverter](http://xstream.codehaus.org/javadoc/com/thoughtworks/xstream/converters/basic/AbstractSingleValueConverter.html)that shields us from so low level mechanisms. And now our class looks much better:
</code> java
public class DaysAgoConverter extends AbstractSingleValueConverter {</p>

<pre><code>@Override
public boolean canConvert(Class type) {
    return type.equals(Integer.class);
}

@Override
public Object fromString(String str) {
    return null;
}

public String toString(Object obj) {
    return null;
}
</code></pre>

<p>}
```
Additionally we must override method toString(Object obj) defined in AbstractSingleValueConverter as we want to store Date in XML calculated from Integer, not a simple <em>Object.toString</em> value which would be returned from default <em>toString</em> defined in abstract parent.</p>

<p><strong>Implementation</strong></p>

<p>Code below is pretty straightforward, but most interesting lines are commented. I&rsquo;ve skipped all validation stuff to make this example shorter.
``` java
public class DaysAgoConverter extends AbstractSingleValueConverter {</p>

<pre><code>private final static String FORMAT = "yyyy-MM-dd"; // default Date format that will be used in conversion
private final DateTime now = DateTime.now().toDateMidnight().toDateTime(); // current day at midnight

public boolean canConvert(Class type) {
    return type.equals(Integer.class);     // Converter works only with Integers
}

@Override
public Object fromString(String str) {
    SimpleDateFormat format = new SimpleDateFormat(FORMAT);
    try {
        Date date = format.parse(str);
        return Days.daysBetween(new DateTime(date), now).getDays();  // we simply calculate days between using JodaTime
    } catch (ParseException e) {
        throw new RuntimeException("Invalid date format in " + str);
    }
}

public String toString(Object obj) {
    if (obj == null) {
        return null;
    }

    Integer daysAgo = ((Integer) obj);
    return now.minusDays(daysAgo).toString(FORMAT); // here we subtract days from now and return formatted date string
}
</code></pre>

<p>}
```</p>

<p><strong>Usage</strong>
To use our custom converter for a specific field we have to inform about it XStream object using registerLocalConverter:
``` java</p>

<pre><code>xstream.registerLocalConverter(Ban.class, "daysAgo", new DaysAgoConverter());
</code></pre>

<p>```
We are using &ldquo;local&rdquo; method to apply this conversion only to specific field and not to every Integer field in XML file. And after that we will get our Ban objects populated with number of days instead of Date.</p>

<h3>Summary</h3>

<p>That&rsquo;s all what I wanted to show you in this post. Now you have basic knowledge about what XStream is capable of and how it can be used to easily map XML data to Java objects If you need something more advanced, please check <a href="http://xstream.codehaus.org/">project official page</a> as it contains very good documentation and examples.</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Running unit tests and integration tests separately with Maven Failsafe and TestNG]]></title>
    <link href="http://tomaszdziurko.pl/2013/01/running-unit-tests-integration-tests-separately-maven-testng/"/>
    <updated>2013-01-23T16:42:28+01:00</updated>
    <id>http://tomaszdziurko.pl/2013/01/running-unit-tests-integration-tests-separately-maven-testng</id>
    <content type="html"><![CDATA[<p>Recently for my new pet project I decided that I would like to have some tests executed during standard <em>mvn test</em> and some other ones only during different phase, let&rsquo;s call it integration phase. I googled and googled and nothing seemed to work, so after struggling with making my setup work I&rsquo;ve decided to write down my findings how I was able to configure TestNG with Maven to run integration and unit tests separately.</p>

<h3>Basic (not working) setup</h3>

<p>For integration testing there is a <a href="http://maven.apache.org/surefire/maven-failsafe-plugin/integration-test-mojo.html">Maven Failsafe plugin</a> that is supposed to do what we want out of the box. Unfortunately, things are not as easy and straightforward as we might expect.</p>

<!-- more -->


<p>Let&rsquo;s look at basic setup that should work:</p>

<p>We have two test classes, one with IT postfix that will indicate that this is our integration test:</p>

<p>``` java
import org.testng.annotations.Test;</p>

<p>import static org.fest.assertions.Assertions.assertThat;</p>

<p>public class ExampleUnitTest {</p>

<pre><code>@Test
public void shouldPass() {
    assertThat(false).isFalse();
}
</code></pre>

<p>}
```</p>

<p>and</p>

<p>``` java
import org.testng.annotations.Test;</p>

<p>import static org.fest.assertions.Assertions.assertThat;</p>

<p>public class StatusTestIT {</p>

<pre><code>@Test
public void shouldFail() {
    assertThat("aaa").isEqualTo("");
}
</code></pre>

<p>}
```</p>

<p>and now let&rsquo;s declare plugin mentioned above in our pom.xml:</p>

<p>``` xml
<project></p>

<pre><code>&lt;!-- ... --&gt;
&lt;build&gt;
    &lt;plugins&gt;
        &lt;!-- ... --&gt;
        &lt;plugin&gt;
            &lt;groupId&gt;org.apache.maven.plugins&lt;/groupId&gt;
            &lt;artifactId&gt;maven-surefire-plugin&lt;/artifactId&gt;
            &lt;version&gt;2.9&lt;/version&gt;
        &lt;/plugin&gt;

        &lt;plugin&gt;
            &lt;groupId&gt;org.apache.maven.plugins&lt;/groupId&gt;
            &lt;artifactId&gt;maven-failsafe-plugin&lt;/artifactId&gt;
            &lt;version&gt;2.13&lt;/version&gt;
            &lt;configuration&gt;
                &lt;includes&gt;
                    &lt;include&gt;**/*IT.java&lt;/include&gt;
                &lt;/includes&gt;
            &lt;/configuration&gt;
        &lt;/plugin&gt;
    &lt;/plugins&gt;
&lt;/build&gt;
</code></pre>

<p></project>
```</p>

<p>After that we should see two things. <em>mvn test</em> should pass as we have a one green test in ExampleUnitTest class, but <em>mvn failsafe:integration-test</em> should fail with StatusTestIT red. But as I said before, this does not work. <em>mvn test</em> looks as expected, but second Maven execution is passing as well showing that no test were run. Plugin seems to omit our completely valid test&hellip;</p>

<h3>Small fix doing its job</h3>

<p>After testing different approaches I found out that all we need to make this setup work is to add plugin execution to an integration-test phase of maven life cycle. So tiny change, but now our integration tests are executed only when we call <em>mvn integration-test</em>.</p>

<p>``` xml
<plugin></p>

<pre><code>&lt;groupId&gt;org.apache.maven.plugins&lt;/groupId&gt;
&lt;artifactId&gt;maven-failsafe-plugin&lt;/artifactId&gt;
&lt;version&gt;2.13&lt;/version&gt;
&lt;configuration&gt;
    &lt;includes&gt;
        &lt;include&gt;**/*IT.java&lt;/include&gt;
    &lt;/includes&gt;
&lt;/configuration&gt;
&lt;executions&gt;
    &lt;execution&gt;
        &lt;id&gt;failsafe-integration-tests&lt;/id&gt;
        &lt;phase&gt;integration-test&lt;/phase&gt;
        &lt;goals&gt;
            &lt;goal&gt;integration-test&lt;/goal&gt;
        &lt;/goals&gt;
    &lt;/execution&gt;
&lt;/executions&gt;
</code></pre>

<p></plugin>
```</p>

<p>And that&rsquo;s all, we could execute integration tests only when we really want to. Of course there are some things to remember when adopting setup described above:</p>

<ul>
<li><p>our unit tests must have Test postfix, if you use something else, we have to configure surefire plugin using <em>include</em></p></li>
<li><p>during integration test unit tests are executed too. This is a small flaw in this setup, but exclude <em>*/</em>Test.java seem not to work :(</p></li>
</ul>


<p>Source code of this example setup is available on <a href="https://github.com/tdziurko/maven-testng-integration-tests">GitHub</a></p>
]]></content>
  </entry>
  
</feed>
